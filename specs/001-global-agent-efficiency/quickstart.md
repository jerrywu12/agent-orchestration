# Validation quickstart
Run Python unittest discovery in config/global-agent-tooling/agent-efficiency, then the existing guardrail suite. Install to a disposable home and repeat; verify unchanged output, config preservation, rollback drift rejection and restoration. Invoke agent-run from existing and fresh project directories on synthetic success/failure output. Compare RTK git output with raw Git output. Start installed Serena through MCP stdio, activate an isolated Python/TS fixture, find symbols/references and verify a changed file. Install to the current user only after tests and config preflight pass. Run harmless local/cloud provider greetings with existing identity only; record external failures without changing auth. Run repository CI smoke and inspect committed diff before push/PR.
