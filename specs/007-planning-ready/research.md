# Decisions
Structured path scopes plus shared identifiers provide deterministic conservative checks; titles cannot prove independence. Reuse SQLite transactions for races. Preserve legacy data but revalidate future execution. Local isolated validation exercises runner and app without disturbing live tickets. HTTPS fetched f7ccbe3 after SSH failed.
