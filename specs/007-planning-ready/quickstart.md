# Validation
From tools/agent-desk run npm test, npm run build, npm run test:e2e. Use existing isolated server/database and fake agents. Verify planning purpose, six stages, cancel, missing fields, owner selection, conflicts, stale confirmations, queue and failed start outcomes. Never start real user tickets as verification.
