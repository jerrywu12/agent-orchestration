# API
Readiness GET and confirmed transition POST use the exact schema in ../plan.md. Existing error.code/error.message shape applies. Preview grants no claim; confirmation and dispatch recheck current state. Scoped tools cannot fabricate user start confirmation.
