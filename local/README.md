# Local State

This directory is for machine-local agent and model state. It is intentionally ignored by git because it can contain credentials, OAuth tokens, SQLite databases, logs, browser profiles, and large model caches.

Tracked docs and scripts in this repo describe the layout. The state itself should stay local.
